import React from "react"
import { searchByName } from "api/index"

const Nav = () => {
  return (
    <div>
      <input type="text" placeholder="Name or Number" />
    </div>
  )
}

export default Nav
